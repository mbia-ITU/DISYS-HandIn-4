syntax = "proto3";

package ping;

option go_package = "./gRPC";

service Ping {
    rpc ping (Request) returns (Reply) {}
}

message Reply {
    string message = 1;
}

message Request {
    int32 id = 1;
    int32 timestamp = 2;
}